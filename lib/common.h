#pragma once

#define null NULL

#include "ints.h"
#include "string.h"
#include "allocators.h"
#include "log.h"

#define for_to(init, upper_boundary) for(int (init) = 0; (init) < (upper_boundary); (init++))